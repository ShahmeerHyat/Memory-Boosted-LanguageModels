version: "3.9"

services:
  pharmchat-api:
    build: .
    container_name: pharmchat-api
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - MODEL_NAME=qwen2.5:7b
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
